import { StyleSheet } from "react-native";

export const input_styles = StyleSheet.create({
    Input: {
        width: "90%",
        padding: 2,
        paddingLeft: 10,
        paddingRight: 10,

        borderColor: "grey",
        borderWidth: 1,
        borderRadius: 10,

    },
});
